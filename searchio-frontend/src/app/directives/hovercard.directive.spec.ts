import { HovercardDirective } from './hovercard.directive';

describe('HovercardDirective', () => {
  it('should create an instance', () => {
    const directive = new HovercardDirective();
    expect(directive).toBeTruthy();
  });
});
